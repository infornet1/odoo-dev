<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
        <!-- Search view of payslip runs -->
    <record id="hr_payslip_run_search" model="ir.ui.view">
        <field name="name">hr.payslip.run.search</field>
        <field name="model">hr.payslip.run</field>
        <field name="arch" type="xml">
            <search string="Search Payslip Batches">
                <field name="name" string="Payslip Batches"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <filter string="Draft" name="draft"
                        domain="[('state','=','draft')]"
                        help="Draft Payslip Batches"/>
                <filter name="done_filter" string="Done"
                        domain="[('state','=','close')]"
                        help="Done Payslip Batches"/>
            </search>
        </field>
    </record>
<!--    Tree view of hr_payslip_run-->
    <record id="hr_payslip_run_view_tree" model="ir.ui.view">
        <field name="name">hr.payslip.run.view.tree</field>
        <field name="model">hr.payslip.run</field>
        <field name="arch" type="xml">
            <tree string="Payslips Batches">
                <field name="name"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="credit_note"/>
                <field name="state"/>
            </tree>
        </field>
    </record>
    <!--    Kanban view of hr_payslip_run-->
    <record id="hr_payslip_run_view_kanban" model="ir.ui.view">
        <field name="name">hr.payslip.run.view.kanban</field>
        <field name="model">hr.payslip.run</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_content oe_kanban_global_click">
                            <div class="row">
                                <div class="col-6">
                                    <strong>
                                        <field name="name"/>
                                    </strong>
                                </div>
                                <div class="col-6">
                                    <span class="float-right badge badge-secondary">
                                        <field name="state"/>
                                    </span>
                                </div>
                                <div class="col-12">
                                    <span>
                                        <field name="date_start"/>
                                        -
                                        <field name="date_end"/>
                                    </span>
                                    <span class="float-right"
                                          title="Is a Blocking Reason?">
                                        <field name="credit_note"
                                               widget="boolean"/>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
<!--   Form view of hr_payslip_run-->
    <record id="hr_payslip_run_view_form" model="ir.ui.view">
        <field name="name">hr.payslip.run.view.form</field>
        <field name="model">hr.payslip.run</field>
        <field name="arch" type="xml">
            <form string="Payslips Batches">
                <header>
                    <button name="close_payslip_run" type="object"
                            string="Close" invisible="state != 'draft'"
                            class="oe_highlight"/>
                    <button name="%(hr_payslip_by_employees_action)d"
                            type="action"
                            attrs="{'invisible': ['|', ('state', '!=', 'draft'), ('can_generate_payslips', '=', False)]}"
                            string="Generate Payslips" class="oe_highlight"/>
                    <button string="Set to Draft" name="action_payslip_run"
                            type="object" invisible="state != 'close'"/>
                    <button string="Validate" name="action_validate_payslips" type="object" class="oe_highlight"
                    invisible="is_validate == False"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <!-- Warning message for unconfirmed rate -->
                    <div class="alert alert-warning" role="alert"
                         attrs="{'invisible': [('exchange_rate_confirmed', '=', True)]}">
                        <strong>Exchange Rate Required:</strong>
                        Please set and confirm the exchange rate before generating payslips.
                    </div>

                    <label for="name" class="oe_edit_only"/>
                    <h1>
                        <field name="name" readonly="state != 'draft'"/>
                    </h1>
                    <group col="4">
                        <label for="date_start" string="Period"
                               readonly="state != 'draft'"/>
                        <div>
                            <field name="date_start" class="oe_inline"/>
                            -
                            <field name="date_end" class="oe_inline"
                                   readonly="state != 'draft'"/>
                        </div>
                        <field name="credit_note" readonly="state != 'draft'"/>
                        <field name="is_validate" invisible="1"/>
                    </group>

                    <!-- Exchange Rate Control Section -->
                    <group string="Exchange Rate Control" name="exchange_control">
                        <group>
                            <field name="batch_exchange_rate"
                                   attrs="{'readonly': [('exchange_rate_confirmed', '=', True)]}"/>
                            <field name="exchange_rate_confirmed" readonly="1"/>
                        </group>
                        <group>
                            <field name="exchange_rate_set_date" readonly="1"
                                   attrs="{'invisible': [('exchange_rate_confirmed', '=', False)]}"/>
                            <field name="exchange_rate_set_by" readonly="1"
                                   attrs="{'invisible': [('exchange_rate_confirmed', '=', False)]}"/>
                        </group>
                        <group colspan="2">
                            <div class="oe_button_box" name="exchange_rate_buttons">
                                <button name="action_confirm_exchange_rate"
                                        string="Confirm Exchange Rate"
                                        type="object"
                                        class="btn-primary"
                                        attrs="{'invisible': ['|', ('exchange_rate_confirmed', '=', True),
                                                                   ('batch_exchange_rate', '&lt;=', 0)]}"/>

                                <button name="action_update_exchange_rate"
                                        string="Update Rate"
                                        type="object"
                                        class="btn-warning"
                                        attrs="{'invisible': [('exchange_rate_confirmed', '=', False)]}"/>
                            </div>
                        </group>
                    </group>

                    <!-- Hidden field for can_generate_payslips -->
                    <field name="can_generate_payslips" invisible="1"/>
                    <separator string="Payslips"/>
                    <field name="slip_ids" readonly="state != 'draft'"/>
                </sheet>
            </form>
        </field>
    </record>
<!--    Window action for hr_payslip_run-->
    <record id="hr_payslip_run_action" model="ir.actions.act_window">
        <field name="name">Payslips Batches</field>
        <field name="res_model">hr.payslip.run</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="search_view_id" ref="hr_payslip_run_search"/>
    </record>
<!--    Menu for hr_payslip-->
    <menuitem action="hr_payslip_run_action" id="menu_hr_payslip_run"
              parent="menu_hr_payroll_community_root"/>

    <!-- Rate Update Wizard View -->
    <record id="hr_payslip_run_rate_update_wizard_form" model="ir.ui.view">
        <field name="name">hr.payslip.run.rate.update.wizard.form</field>
        <field name="model">hr.payslip.run.rate.update.wizard</field>
        <field name="arch" type="xml">
            <form string="Update Exchange Rate">
                <group>
                    <div class="alert alert-warning" role="alert"
                         attrs="{'invisible': [('has_payslips', '=', False)]}">
                        <i class="fa fa-warning"/> Warning: This batch has
                        <field name="payslips_count" readonly="1" class="oe_inline"/>
                        generated payslips.
                    </div>
                </group>

                <group>
                    <field name="current_rate" readonly="1"/>
                    <field name="new_rate" required="1"/>
                </group>

                <group attrs="{'invisible': [('has_payslips', '=', False)]}">
                    <field name="regenerate_payslips"/>
                    <div class="text-muted">
                        If checked, existing payslips will be deleted and you can regenerate with the new rate.
                        If unchecked, existing payslips will be updated with the new rate.
                    </div>
                </group>

                <!-- Hidden fields -->
                <field name="payslip_run_id" invisible="1"/>
                <field name="has_payslips" invisible="1"/>

                <footer>
                    <button name="action_confirm_update"
                            string="Confirm Update"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>


</odoo>
