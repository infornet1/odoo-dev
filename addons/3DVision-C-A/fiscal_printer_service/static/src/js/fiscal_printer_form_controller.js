/** @odoo-module **/

import { FormController } from "@web/views/form/form_controller";

export class FiscalPrinterFormController extends FormController {
    // This controller is now simplified since we're using client actions
    // The buttons will call the model methods which return client actions
} 