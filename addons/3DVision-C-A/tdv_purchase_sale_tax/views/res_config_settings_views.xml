<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_account_res_config_settings_form" model="ir.ui.view">
    <field name="name">account_res_config_settings_form_inherit</field>
    <field name="model">res.config.settings</field>
    <field name="inherit_id" ref="account.res_config_settings_view_form"/>
    <field name="arch" type="xml">
      <xpath expr="//block[@name='default_taxes_setting_container']" position="inside">
        <setting id="show_sale_tax" help="Enable the visualization of the sales tax and control if the sales taxes in the transactions must be displayed.">
          <field name="enable_sale_tax"/>
        </setting>
        <setting id="show_purchase_tax" help="Enable the visualization of supplier retention and controls if tax withholdings in purchase transactions must be shown.">
          <field name="enable_purchase_tax"/>
        </setting>
        
        <setting id="show_recalculate_prices" help="Enabling this option will recalculate the product prices on the customer invoice excluding the tax. This will adjust the price of each invoice line to reflect the base amount before applying the selected tax.">
          <field name="enable_recalculate_prices"/>
        </setting>
      </xpath>
    </field>
  </record>

  

</odoo>
 