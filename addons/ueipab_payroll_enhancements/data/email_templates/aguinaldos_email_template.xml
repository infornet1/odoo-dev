<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- ========================================
             AGUINALDOS Email Template - Christmas Bonus
             ======================================== -->

        <record id="email_template_aguinaldos_delivery" model="mail.template">
            <field name="name">Aguinaldos Email - Christmas Bonus Delivery</field>
            <field name="model_id" ref="hr_payroll_community.model_hr_payslip"/>
            <field name="subject">🎄 Aguinaldos (Bono Navideño) - ${object.employee_id.name}</field>
            <field name="email_from">recursoshumanos@ueipab.edu.ve</field>
            <field name="email_to">${object.employee_id.work_email or object.employee_id.address_home_id.email or ''}</field>
            <field name="auto_delete" eval="True"/>

        <!-- Optional: Attach PDF payslip report -->
        <!-- <field name="report_template" ref="hr_payroll.action_report_payslip"/> -->
        <!-- <field name="report_name">Aguinaldos_${object.employee_id.name}_${object.date_from.strftime('%Y%m%d') if object.date_from else 'aguinaldos'}</field> -->

            <field name="body_html" type="html"><![CDATA[
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Aguinaldos (Bono Navideño) - UEIPAB</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        .payslip-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #c94b4b 0%, #4b134f 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .header p {
            margin: 5px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }
        .content {
            padding: 30px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        .info-label {
            font-weight: 600;
            color: #555;
            font-size: 14px;
        }
        .info-value {
            color: #333;
            font-size: 14px;
        }
        .section-title {
            background: linear-gradient(135deg, #c94b4b 0%, #4b134f 100%);
            color: white;
            padding: 12px 20px;
            margin: 25px -30px 15px -30px;
            font-size: 18px;
            font-weight: 600;
        }
        .amount-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        .amount-table td {
            padding: 10px;
            border-bottom: 1px solid #e0e0e0;
        }
        .amount-table .label-col {
            color: #555;
            font-weight: 500;
            width: 60%;
        }
        .amount-table .amount-col {
            text-align: right;
            color: #333;
            font-weight: 600;
            font-size: 15px;
        }
        .total-section {
            background: #fff5f5;
            padding: 20px;
            margin: 25px -30px;
            border-top: 3px solid #c94b4b;
            border-bottom: 3px solid #c94b4b;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            font-size: 16px;
        }
        .total-row.final {
            font-size: 20px;
            font-weight: bold;
            color: #c94b4b;
        }
        .bcv-section {
            background: #fff9e6;
            border: 2px dashed #ffc107;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
        }
        .bcv-section .bcv-label {
            font-size: 14px;
            color: #856404;
            margin-bottom: 5px;
        }
        .bcv-section .bcv-rate {
            font-size: 24px;
            font-weight: bold;
            color: #d39e00;
        }
        .footer {
            background: #f8f9fa;
            padding: 20px 30px;
            border-top: 1px solid #e0e0e0;
            text-align: center;
            font-size: 13px;
            color: #666;
            line-height: 1.6;
        }
        .footer strong {
            color: #c94b4b;
        }
        .christmas-note {
            background: #fff5f5;
            border-left: 4px solid #c94b4b;
            padding: 15px;
            margin: 20px 0;
            font-style: italic;
            color: #666;
        }
        @media (max-width: 600px) {
            .payslip-container {
                margin: 0;
                border-radius: 0;
            }
            .content {
                padding: 20px;
            }
            .section-title {
                margin: 20px -20px 10px -20px;
                font-size: 16px;
            }
            .total-section {
                margin: 20px -20px;
            }
        }
    </style>
</head>
<body>
    <div class="payslip-container">
        <!-- Header -->
        <div class="header">
            <h1>🎄 Aguinaldos (Bono Navideño)</h1>
            <p>Instituto Privado Andrés Bello, CA</p>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Employee Information -->
            <div class="info-row">
                <span class="info-label">📋 Nro. Comprobante:</span>
                <span class="info-value">${object.number or 'N/A'} │ ${object.payslip_run_id.name if object.payslip_run_id else 'N/A'}</span>
            </div>
            <div class="info-row">
                <span class="info-label">📅 Período:</span>
                <span class="info-value">${object.date_from.strftime('%d/%m/%Y') if object.date_from else 'N/A'} → ${object.date_to.strftime('%d/%m/%Y') if object.date_to else 'N/A'}</span>
            </div>
            <div class="info-row">
                <span class="info-label">👤 Nombre y Apellido:</span>
                <span class="info-value">${object.employee_id.name or 'N/A'}</span>
            </div>
            <div class="info-row">
                <span class="info-label">🆔 Cédula:</span>
                <span class="info-value">${object.employee_id.identification_id or 'N/A'}</span>
            </div>
            <div class="info-row">
                <span class="info-label">💳 Cuenta:</span>
                <span class="info-value">${object.employee_id.bank_account_id.acc_number if object.employee_id.bank_account_id else 'N/A'}</span>
            </div>

            <%
                # Get AGUINALDOS line (maps to Salario Base display)
                aguinaldos = object.line_ids.filtered(lambda l: l.code == 'AGUINALDOS')

                # For AGUINALDOS, the entire amount goes to "Salario Base" display
                # No bonos, no extrabonus, no deductions
                aguinaldos_amount = aguinaldos.total if aguinaldos else 0.0

                # Get exchange rate (use payslip field or default)
                exchange_rate = getattr(object, 'exchange_rate_used', None) or 241.5780
            %>

            <!-- Christmas Note -->
            <div class="christmas-note">
                🎁 <strong>Aguinaldos:</strong> Bono navideño equivalente a 2 meses de salario base,
                pagado en dos cuotas (50% en cada quincena de diciembre).
            </div>

            <!-- Aguinaldos Amount Section -->
            <div class="section-title">🎁 Aguinaldos (Bono Navideño)</div>
            <table class="amount-table">
                <tr>
                    <td class="label-col">Aguinaldos - Período ${object.date_from.strftime('%d/%m/%Y') if object.date_from else 'N/A'} al ${object.date_to.strftime('%d/%m/%Y') if object.date_to else 'N/A'}</td>
                    <td class="amount-col">Bs. ${'{:,.2f}'.format(aguinaldos_amount * exchange_rate)}</td>
                </tr>
                <tr style="border-top: 2px solid #c94b4b; font-weight: bold;">
                    <td class="label-col">Total Aguinaldos</td>
                    <td class="amount-col">Bs. ${'{:,.2f}'.format(aguinaldos_amount * exchange_rate)}</td>
                </tr>
            </table>

            <!-- Note: No Deductions for AGUINALDOS -->
            <div class="christmas-note">
                ℹ️ <strong>Nota:</strong> El pago de Aguinaldos no tiene deducciones legales
                (IVSS, FAOV, INCES, ARI). El monto mostrado es el total a recibir.
            </div>

            <!-- Total Section -->
            <div class="total-section">
                <div class="total-row final">
                    <span>💵 Total a Recibir:</span>
                    <span>Bs. ${'{:,.2f}'.format(aguinaldos_amount * exchange_rate)}</span>
                </div>
            </div>

            <!-- Exchange Rate -->
            <div class="bcv-section">
                <div class="bcv-label">Tasa de Cambio (Referencia)</div>
                <div class="bcv-rate">Bs. ${'{:,.4f}'.format(exchange_rate)}</div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p><strong>🎄 ¡Felices Fiestas!</strong></p>
            <p><strong>📧 ¿Dudas o consultas?</strong></p>
            <p>Si usted tiene alguna duda por favor escribir inmediatamente al correo <strong>recursoshumanos@ueipab.edu.ve</strong></p>
            <p style="margin-top: 15px; color: #999; font-size: 12px;">
                Este es un documento generado automáticamente. Por favor, responda directamente a este correo si tiene dudas.
            </p>
        </div>
    </div>
</body>
</html>
]]></field>
        </record>

    </data>
</odoo>
